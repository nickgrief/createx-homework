import { DimDirective } from './dim.directive';

describe('DimDirective', () => {
  it('should create an instance', () => {
    const directive = new DimDirective();
    expect(directive).toBeTruthy();
  });
});
